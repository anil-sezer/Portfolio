---
apiVersion: v1
kind: Service
metadata:
  name: postgres-svc-clusterip
  namespace: database
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: internal
      protocol: TCP
      port: 5432
      targetPort: 5432
