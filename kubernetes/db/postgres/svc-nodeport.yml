---
apiVersion: v1
kind: Service
metadata:
  name: postgres-nodeport
  namespace: database
spec:
  type: NodePort
  selector:
    app: postgres
  ports:
    - name: external
      protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30001
